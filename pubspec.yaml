name: new_matule  # твое имя проекта
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  ui_kit:
    git:
      url: https://github.com/pffkilm/MatuleS.git
      ref: ui_fixed
      path: packages/ui_kit


  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/